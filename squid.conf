#
# Recommended minimum configuration (Squid upstream defaults), plus ICAP content
# adaptation for antivirus scanning via c-icap + ClamAV (see clamd_mod.conf /
# virus_scan.conf).
#

acl localnet src 0.0.0.1-0.255.255.255	# RFC 1122 "this" network (LAN)
acl localnet src 10.0.0.0/8		# RFC 1918 local private network (LAN)
acl localnet src 100.64.0.0/10		# RFC 6598 shared address space (CGN)
acl localnet src 169.254.0.0/16 	# RFC 3927 link-local (directly plugged) machines
acl localnet src 172.16.0.0/12		# RFC 1918 local private network (LAN)
acl localnet src 192.168.0.0/16	# RFC 1918 local private network (LAN)
acl localnet src fc00::/7       	# RFC 4193 local private network range
acl localnet src fe80::/10     	# RFC 4291 link-local (directly plugged) machines
__SQUID_EXTRA_LOCALNET_ACLS__

acl SSL_ports port 443
acl Safe_ports port 80		# http
acl Safe_ports port 21		# ftp
acl Safe_ports port 443		# https
acl Safe_ports port 70		# gopher
acl Safe_ports port 210		# wais
acl Safe_ports port 1025-65535	# unregistered ports
acl Safe_ports port 280	# http-mgmt
acl Safe_ports port 488	# gss-http
acl Safe_ports port 591	# filemaker
acl Safe_ports port 777	# multiling http
acl CONNECT method CONNECT

http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
http_access allow localhost manager
http_access deny manager
http_access allow localnet
http_access allow localhost
http_access deny all

visible_hostname __SQUID_VISIBLE_HOSTNAME__
__SQUID_HTTP_PORT_LINE__

cache_mem __SQUID_CACHE_MEM_MB__ MB
maximum_object_size __SQUID_MAX_OBJECT_SIZE_MB__ MB

coredump_dir /var/cache/squid

#
# SSL-bump (TLS interception), off unless ENABLE_SSL_BUMP=true.
# When enabled, HTTPS traffic is decrypted with an on-the-fly certificate
# signed by our own CA (see cont-init.d/04-ssl-bump-init) so it can be scanned
# by the same ICAP antivirus pipeline as plain HTTP. Clients must trust that
# CA certificate, or every HTTPS site will show a certificate warning.
#
__SSL_BUMP_CONFIG__

refresh_pattern ^ftp:		1440	20%	10080
refresh_pattern ^gopher:	1440	0%	1440
refresh_pattern -i (/cgi-bin/|\?) 0	0%	0
refresh_pattern \/(Packages|Sources)(|\.bz2|\.gz|\.xz)$ 0 0% 0 refresh-ims
refresh_pattern \/Release(|\.gpg)$ 0 0% 0 refresh-ims
refresh_pattern \/InRelease$ 0 0% 0 refresh-ims
refresh_pattern \/(Translation-.*)(|\.bz2|\.gz|\.xz)$ 0 0% 0 refresh-ims
refresh_pattern .		0	20%	4320

#
# Antivirus content adaptation (c-icap + ClamAV).
# c-icap listens locally on 127.0.0.1:1344 and exposes the "avscan" service
# (see clamd_mod.conf / virus_scan.conf). bypass=off means requests are
# blocked (fail-closed) rather than passed through unscanned if c-icap/clamd
# is unreachable; set ICAP_BYPASS=on to fail open instead.
# Set ENABLE_ANTIVIRUS=false to disable scanning entirely (icap_enable off).
#
icap_enable __ICAP_ENABLE__
icap_send_client_ip on

icap_service avscan_req reqmod_precache icap://127.0.0.1:1344/avscan bypass=__ICAP_BYPASS__
adaptation_access avscan_req allow all

icap_service avscan_resp respmod_precache icap://127.0.0.1:1344/avscan bypass=__ICAP_BYPASS__
adaptation_access avscan_resp allow all
